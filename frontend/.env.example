# If backend is behind the same public domain and reverse-proxied (recommended),
# leave NEXT_PUBLIC_API_URL empty and requests will use relative paths.
NEXT_PUBLIC_API_URL=

# Optional: static CDN domain for Next production assets (/_next/static/*).
NEXT_PUBLIC_CDN_STATIC_BASE_URL=
